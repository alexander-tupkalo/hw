// URL до API
const API_URL = "https://api.something.com";

// Максимальна кількість спроб входу
const MAX_LOGIN_ATTEMPTS = 5;

// Основний колір
const PRIMARY_COLOR = "#FFFFFF";

// Назва проєкту
const PROJECT_NAME = "Homework 4";

// Мова за замовчуванням
const DEFAULT_LANGUAGE = "uk";

// Виведення в консоль
console.log(API_URL);
console.log(MAX_LOGIN_ATTEMPTS);
console.log(PRIMARY_COLOR);
console.log(PROJECT_NAME);
console.log(DEFAULT_LANGUAGE);